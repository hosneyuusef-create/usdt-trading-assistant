# Test Environment Variables
# Based on tests/README.md (line 9) and test scripts

# PostgreSQL Database (from tests/data/seed/seed.ps1 lines 9-12)
PGHOST=localhost
PGPORT=5432
PGDATABASE=usdt_trading
PGUSER=postgres
# PGPASSWORD stored in Windows Credential Manager as secret `USDT_POSTGRES_PWD`
